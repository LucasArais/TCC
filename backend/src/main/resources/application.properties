spring.application.name=TCC 

# Conexão com PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/tcc
spring.datasource.username=postgres
# Senha configurada como variável de ambiente
spring.datasource.password=${DB_PASSWORD}

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# SMTP - Recuperação de senha por e-mail

#substitua o campo abaixo pelo valor do mail.trap
spring.mail.host=sandbox.smtp.mailtrap.io
spring.mail.port=2525

#substitua o campo abaixo pelo valor do mail.trap
spring.mail.username=f65a9bef126a7d

#substitua o campo abaixo pelo valor do mail.trap
spring.mail.password=e313fc5b93ab74
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.from=projetotcc@example.com